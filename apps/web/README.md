# Web Frontend

Placeholder for the future authenticated management UI and public read-only view.

Planned stack:

- Next.js or React;
- TypeScript;
- Tailwind CSS;
- shadcn/ui or equivalent component system;
- AG Grid Community for spreadsheet-like DHCP/IPAM tables.

The public no-login view must be a separate sanitized published snapshot path, not the authenticated admin API with edit controls hidden.
