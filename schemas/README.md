# Schemas

Shared schema contracts for the control plane, device gateway, Pi agent, apply helper, and public snapshot publication.

Current contents:

- `config-artifact.schema.json`: preliminary JSON Schema for signed desired-state config artifacts.

Schemas are intentionally preliminary and should evolve with tests as implementation begins.
